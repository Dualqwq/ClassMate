# ClassMate MVP 进展

> 最后更新：2026-07-23  
> 当前阶段：阶段 A 已完成，进入阶段 B/C 联调与人工验收；同步规划上下文补强与 LangGraph API 流程重构  
> 对应分支：`codex/phase-a-context-pipeline`  
> 对应提交：`7001f15`  
> 产品定义：[产品.md](./产品.md)  
> Skill 专项进展：[Skill进展.md](./Skill进展.md)

## 1. MVP 目标

ClassMate 面向低年级 C/C++ 与 OOP 课程学生，第一版需要跑通以下最小使用闭环：

```text
打开作业
→ 读取 question.md、当前代码和选中代码
→ 学生通过对话或快捷按钮求助
→ Skill 给出符合课程难度的分层提示、讲解或 Debug 建议
→ 学生修改并重新编译/运行
→ 插件记录问题和修改过程
→ 学生手动导出当前题目的 Markdown 错题本
```

## 2. 当前结论

项目已经具备侧边栏对话、模型接入、教学 Skill、代码选区解释、编译错误解析、Debug 历程记录和错题本导出等主要模块。

阶段 A 的上下文入口已在代码层完成：插件能够按当前作业目录优先读取题目，向模型提供当前文件、选区、语言、工作区代码和会话内代码变化，并发送当前会话的全部有效历史；C/C++ 识别与典型同目录多文件编译也已补齐。

目前尚未完成全部 MVP 人工验收。剩余重点集中在四类教学快捷入口、运行错误采集与超时处理、单题错题本范围控制、扫描 PDF OCR、多文件自动修改，以及安装新版 VSIX 后的端到端回归测试。

PR1 review 还发现了三类需要补强的上下文问题：仅大小写不同的作业目录可能导致 `question.md` 读串、OOP 作业中的 `Makefile` 尚未进入上下文、`CLASSMATE.md` 依赖初学者手写 YAML 的使用门槛过高。此外，当前 API 调用、提示词构建和工作区上下文之间存在较多分散的判断逻辑，后续将使用 LangGraph 统一改写为可测试、可观测的多步骤调用流程。

## 3. 已完成内容

### 3.1 对话与界面

- [x] VS Code 侧边栏对话界面
- [x] 支持在侧边栏与编辑器面板之间切换
- [x] 流式显示模型回答
- [x] Markdown 渲染和代码高亮
- [x] 保存当前聊天界面的消息和输入草稿状态
- [x] 支持根据回答类型选择侧边栏或大面板
- [x] 支持创建、切换多个会话，并按首条用户消息生成标题
- [x] 显示会话相对日期
- [x] 按工作区持久保存会话，退出并重启 VS Code 后可恢复
- [x] 消息支持文件、选区和附件引用，文件引用可跳转到对应位置
- [x] 通过加号上传图片、文本、代码和 PDF 附件
- [x] 移除“最多上传 3 张图片”的数量限制

### 3.2 模型接入

- [x] OpenAI 适配器
- [x] Claude 适配器
- [x] DeepSeek 适配器
- [x] 模型名称和 API 地址配置
- [x] API Key 配置与存储
- [x] 模型调用失败时的错误提示
- [x] OpenAI 兼容接口和 Claude 接口支持图片多模态消息
- [x] 请求携带当前会话全部有效历史，不再固定裁剪为 12 条
- [x] 请求并解析 DeepSeek KV Cache 命中与未命中 Token 统计

### 3.3 教学能力

- [x] 内置面向初学者的教学 Skill
- [x] 支持题意理解、解题提示、概念讲解和代码解释
- [x] 支持编译错误、运行错误、错误输出和 OJ 失败等问题类型
- [x] 建立“方向 → 步骤 → 伪代码/骨架 → 完整代码”的提示梯度
- [x] 内置基础知识、常见误区、错误指南和回答模板

Skill 的详细覆盖范围和后续工作统一记录在 [Skill进展.md](./Skill进展.md)。

### 3.4 代码与编译

- [x] 读取用户选中的代码
- [x] 为选中代码显示 `Explain` 行内入口
- [x] 调用本机 `g++` 编译当前 C++ 文件
- [x] 编译前保存当前文件
- [x] 展示编译退出码、耗时、标准输出和错误输出
- [x] 解析 GCC/G++ 报错中的文件、行号和列号
- [x] 允许选中编译输出并请求解释
- [x] 支持 C 与 C++ 文件，Markdown 和普通文本不会触发 C/C++ 编译逻辑
- [x] 支持编译同一作业目录中的多个 `.c/.cc/.cpp/.cxx` 源文件
- [x] 将当前活动文件全文、文件名、语言和选区加入统一上下文
- [x] 将工作区内 C/C++ 源文件和头文件内容加入模型上下文
- [x] 记录会话期间文件新增、删除、插入、删除和替换位置
- [x] 识别代码修改意图并生成“应用到当前文件”操作
- [x] 应用代码前检查文件是否在模型响应期间发生变化

### 3.5 Debug 历程与错题本

- [x] 记录编译错误、编译成功、求助和代码修改事件
- [x] 保存代码修改前后的快照和 diff
- [x] 为相似错误生成错误指纹并追踪错误生命周期
- [x] 生成知识点卡片和基础学习统计
- [x] 在 Debug Journey 树中查看调试历程
- [x] 手动导出 Markdown Debug 错题本
- [x] 模型不可用时使用本地模板导出

### 3.6 题目、附件与 PDF

- [x] 修正 `question.md` 搜索和文件监听规则
- [x] 优先读取当前代码所在作业目录的 `question.md`
- [x] 支持同目录 `question.pdf` 作为题目来源
- [x] 支持提取带文本层 PDF 的文字并加入模型上下文
- [x] 上传的可读文本和代码附件会直接加入模型上下文
- [x] 扫描型 PDF 无可用文本时给出需要 OCR 的明确提示
- [ ] 扫描型 PDF 和普通图片的本地 OCR

### 3.7 阶段 A：上下文入口（已完成）

- [x] 修正 `question.md` 的搜索和文件监听规则
- [x] 优先读取当前代码所在作业目录中的 `question.md`
- [x] 修复 C/C++ 智能识别对 Markdown 文件的误判和报错
- [x] 为语言识别增加 C、C++、Markdown 和普通文本的回归测试
- [x] 将当前活动文件内容加入模型上下文
- [x] 将当前选区、文件名和语言类型加入统一上下文对象
- [x] 支持典型多文件 C++ 作业编译
- [x] 记录之前的对话，不让每次对话后前面对话消失
- [x] 将当前会话全部有效历史传给模型，不再固定限制 12 条
- [x] 按工作区持久保存会话，重启 VS Code 后恢复
- [x] 记录工作区代码的新增、删除和位置变化

> 阶段 A 已完成代码实现和自动测试；带“待 VSIX 验收”的项目仍需安装最新插件包进行端到端人工验证。


## 4. 已知问题与 Bug

### P0：阻塞 MVP 主流程

| 编号 | 问题 | 当前表现 | 影响 | 状态 |
|---|---|---|---|---|
| MVP-001 | `question.md` 读取规则错误 | 已改为搜索 `**/question.md`，并优先使用当前代码同目录题目 | 题目上下文入口恢复 | 已修复，待 VSIX 验收 |
| MVP-002 | 普通对话没有当前代码正文 | 已加入活动文件全文、文件名、语言、选区及工作区代码 | 模型能够基于真实代码回答 | 已实现，待 VSIX 验收 |
| MVP-003 | 模型请求没有携带历史对话 | 已发送当前会话全部有效历史，并持久化多会话 | 连续追问可获得上文 | 已实现，待长会话验收 |
| MVP-004 | C/C++ 智能识别误判 Markdown | 已增加语言门控及回归测试 | Markdown 不再进入 C/C++ 流程 | 已修复，自动测试通过 |

### P1：影响典型 OOP 作业体验

| 编号 | 问题 | 当前表现 | 影响 | 状态 |
|---|---|---|---|---|
| MVP-005 | 只编译当前单个 `.cpp` 文件 | 已收集同目录 C/C++ 源文件参与编译 | 典型多文件作业可编译 | 已修复，需注意多个 `main` 的目录 |
| MVP-006 | 缺少 `question.md` 时没有稳定的显式引导 | 支持 `question.md` 和 `question.pdf`，缺失时仍需完善界面级提示 | 用户可能不知道如何补充题目 | 部分完成 |
| MVP-007 | 当前文件及相关文件选择策略缺失 | 当前读取工作区 C/C++ 源文件与头文件全文 | 上下文更完整，但大型项目可能超出预算 | MVP 已实现，后续增加 Token 裁剪 |
| MVP-008 | 扫描 PDF 无法提取题目文字 | 当前只能检测文本层近乎为空并提示需要 OCR | 扫描版题目仍无法自动阅读 | 待实现 OCR |
| MVP-009 | 自动代码修改只支持当前文件 | 模型一次只能生成并应用当前活动文件的完整替换内容 | 无法一次完成跨文件重构 | MVP 后续项 |
| MVP-010 | 仅大小写不同的作业目录可能导致题目读串 | 候选路径按不区分大小写的方式比较后再按字典序选择，当前活动目录不一定优先 | 多题工作区可能向模型提供错误题目 | 待复现、修复并补充回归测试 |
| MVP-011 | OOP 作业的 `Makefile` 未加入上下文 | 当前只收集 C/C++ 源文件、头文件、题目文件和 `CLASSMATE.md` | 模型无法了解真实构建目标、编译参数和文件关系 | 待实现 |
| MVP-012 | `CLASSMATE.md` 依赖规范 YAML，初学者使用门槛高 | 课程标题等信息只有按约定填写 YAML 才能结构化读取 | 用户未按模板填写时，上下文信息缺失或功能形同虚设 | 待重新设计 |

### P2：架构与可维护性风险

| 编号 | 问题 | 当前表现 | 影响 | 状态 |
|---|---|---|---|---|
| MVP-013 | API 调用流程由分散的手写判断逻辑驱动 | `*Adapter.ts`、`systemPromptBuilder.ts`、`workspaceContextProvider.ts` 分别承担上下文判断、提示词拼装和模型调用 | 多步调用难以统一扩展，异常处理、状态追踪和测试边界不清晰 | 计划使用 LangGraph 重构 |



## 5. 接下来要做

### 阶段 B：打通教学对话

- [x] 将当前会话全部有效对话传给模型
- [ ] 补齐“解释 / 没思路 / 看不懂 / 查错误”四个入口
- [ ] 将四个入口与明确的请求类型和 Skill 工作流绑定
- [ ] 用新版 VSIX 验证“方向提示 → 更具体步骤 → 代码骨架”的连续效果
- [ ] 用新版 VSIX 验证没有明确请求时不会直接输出完整答案

### 阶段 C：打通编译与 Debug

- [ ] 区分编译错误、链接错误和插件编译配置错误
- [ ] 为非交互程序增加可捕获输出的运行模式
- [ ] 记录运行退出码、标准错误、崩溃和超时
- [ ] 将运行错误自动关联到 Debug Journey 和错误解释入口

### 阶段 D：补强上下文与 API 调用流程

- [ ] 复现仅大小写不同的作业目录导致 `question.md` 读串的问题
- [ ] 调整题目选择策略：优先选择当前活动文件所在目录中的精确路径，避免用不区分大小写的比较结果替代真实路径
- [ ] 为同名、仅大小写不同目录和多题工作区补充题目选择回归测试
- [ ] 将当前作业目录中的 `Makefile` 纳入上下文，并设置文件大小与数量限制
- [ ] 验证模型能够根据 `Makefile` 识别构建目标、源文件关系和关键编译参数
- [ ] 重新设计 `CLASSMATE.md` 读取策略，不再要求初学者必须编写 YAML
- [ ] 对比“正则提取 / NLP 提取 / 原文直接入模”三种方案，并确定 MVP 默认降级策略
- [ ] 对无法结构化解析的 `CLASSMATE.md` 保留原文上下文，避免静默丢失信息
- [ ] 利用 LangGraph 改写 API 调用流程，逐步替换 `*Adapter.ts`、`systemPromptBuilder.ts` 和 `workspaceContextProvider.ts` 中分散的流程判断
- [ ] 定义统一的 LangGraph 状态对象，明确题目、代码、选区、附件、会话历史、请求类型和模型响应的输入输出边界
- [ ] 将“收集上下文 → 识别请求 → 构建提示词 → 调用模型 → 解析响应 → 记录 Debug Journey”拆分为可独立测试的节点
- [ ] 在 LangGraph 流程中统一流式输出、超时、重试、取消、模型降级和错误提示
- [ ] 增加节点级日志与调用链追踪，并为正常调用、缺失上下文、模型失败和多步调用补充测试
- [ ] 保留现有调用链作为迁移期回退方案，完成行为对比和 VSIX 验收后再切换默认流程


## 6. MVP 验收标准

准备一个包含以下文件的测试作业：

```text
homework-01/
├── question.md
├── main.cpp
├── student.h
└── student.cpp
```

满足以下条件后，视为 MVP 闭环完成：

- [x] 打开 `question.md` 不会触发 C/C++ 功能或产生报错（自动测试通过，待 VSIX 手测）
- [x] 插件能够自动读取已识别的题目和当前代码并加入模型上下文（待 VSIX 手测）
- [ ] 点击“没思路”后只给出第一层方向提示
- [x] 用户说“再具体一点”时会携带上一轮历史（回答质量待 VSIX 手测）
- [ ] 选中代码后可以选择解释、看不懂和查错误
- [x] 插件能将同目录多个 `.cpp` 文件加入编译命令（实际工具链待 VSIX 手测）
- [ ] 编译错误能够定位、解释并记录到 Debug Journey
- [ ] 非交互程序的运行错误能够被捕获并解释
- [ ] 修改代码并重新编译后，插件能记录修改 diff 和修复结果
- [ ] 手动导出的 Markdown 错题本只包含当前题目内容
- [ ] 在未配置模型、缺少 `g++` 或缺少 `question.md` 时有清晰提示
- [ ] 工作区存在名称仅大小写不同的作业目录时，插件始终读取当前活动文件同目录的题目
- [ ] 当前作业含 `Makefile` 时，模型上下文能够获得其有效内容，且不会无界读取大型构建文件
- [ ] `CLASSMATE.md` 不含 YAML 时仍可提取或保留有效课程信息，并向模型提供可用上下文
- [ ] LangGraph 新流程与现有模型供应商兼容，支持流式响应、取消、超时和失败提示
- [ ] LangGraph 各节点具备独立测试，关键调用可追踪，迁移前后核心教学回答行为无明显回退
- [x] 最新源码通过 TypeScript 编译、生产构建和适配器测试；Lint 为 0 错误、6 条既有警告
- [ ] 使用最新提交重新生成并安装 VSIX，完成端到端验收

## 7. 暂不纳入本轮 MVP

- 自动代码补全
- 代码悬浮提示
- 独立 PPT 解析器
- 扫描 PDF 和图片的本地 OCR（文本层 PDF 解析已完成）
- 跨多个文件的自动代码修改
- 超长会话按 Token 预算裁剪（当前发送全部有效历史）
- 多院系定制版本
- 自动错题推送
- 长期学习画像扩展
- 更复杂的工程构建系统自动识别

现有的统计、知识卡和长期画像相关代码可以保留，但在上述主闭环完成前不继续扩展。

## 8. 更新记录

### 2026-07-23

- 根据 PR1 review 新增题目路径大小写冲突、`Makefile` 上下文缺失和 `CLASSMATE.md` 解析门槛三项待办
- 增加对应的复现、修复、回归测试与 MVP 验收标准
- 新增“利用 LangGraph 改写 API 调用流程”事项，覆盖统一状态、节点拆分、异常处理、日志追踪、测试和渐进迁移
- 本次仅更新进展与后续计划，PR1 现有实现暂时保留

### 2026-07-20

- 完成阶段 A：题目搜索、文件监听、活动文件与选区上下文、语言门控、多文件编译和多轮历史
- 会话历史改为上传当前会话全部有效消息，移除固定 12 条限制
- 新增 DeepSeek KV Cache 使用统计，并稳定提示词前缀以提高缓存命中机会
- 新增工作区 C/C++ 代码全文上下文和会话内实时修改时间线
- 新增按工作区持久化的多会话、标题、日期和文件引用
- 新增图片、文本、代码和 PDF 附件；移除最多 3 张图片限制
- 新增 `question.pdf` 与上传 PDF 的文本层解析；扫描 PDF 仅检测并提示，OCR 尚未实现
- 新增当前文件代码替换提案与安全应用操作；跨文件修改尚未实现
- 新增语言、题目选择、编译参数、附件和 DeepSeek 缓存回归测试
- 阶段 A 代码已提交并推送至 `codex/phase-a-context-pipeline`，提交为 `7001f15`

### 2026-07-16

- 创建 MVP 进展文档
- 汇总当前已实现模块
- 记录 `question.md` 读取、代码上下文、多轮对话和多文件编译等问题
- 补充“C/C++ 智能识别导致阅读 Markdown 报错”问题
- 将教学 Skill 进展拆分到独立文档
