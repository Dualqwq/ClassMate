# ClassMate V5 MVP 进展

> 版本：`0.0.5`
>
> 本次 GitHub PR 整理目录：`ClassMate-V5-20260801-GitHub-PR`
>
> 建立日期：2026-07-28
>
> 最近更新：2026-08-01

## 本轮目标

提高作业和期末复习场景下的回答依据，减少“工作区明明有题目和代码，但模型没有读取”的情况。

## 已完成

- [x] 从当前第四版完整复制出独立 V5 目录，原第四版保持不动。
- [x] 增加 `ContextMode`，把问题语义类型和文件加载范围分开。
- [x] 对含题目或活动代码的工作区增加作业场景偏置。
- [x] 思路类、题意类和完整解法类问题使用 `problem_context`。
- [x] 调试类问题使用 `debug_context`。
- [x] 取消工作区文件最多 5 个的固定限制。
- [x] 依据题目根目录加载全部受支持的相关文件。
- [x] 支持 `question/problem/问题/题目/作业说明/assignment` 题目文件，并按当前文件的目录距离选择最近项。
- [x] 支持 C/C++ 源文件、头文件、Makefile、`.mk`、`.txt`、`.in`、`.out`、Markdown 和 PDF。
- [x] 排除压缩包、二进制文件、生成目录和其他题目目录。
- [x] 保存每个会话上一轮的题目根目录、相关文件、问题类型和上下文模式；未明确换题时默认继承。
- [x] 保留单文件大小和总内容大小限制，避免上下文失控。
- [x] 当前候选范围（无题目和活动文件时为整个工作区）不超过 20 个文件且不超过 300 KiB 时，首轮 RouteAndPlan 接收全部受支持文件正文。
- [x] 大型范围中，活动文件直接目录不超过 10 个文件且不超过 200 KiB 时，首轮接收该目录正文。
- [x] 首轮 RouteAndPlan 增加“是否为作业目录、作业根目录、判断依据”结构化输出。
- [x] 模型不能取消控制器确定性选择的题目文件、活动文件和小型作业上下文。
- [x] 已判断为作业但最终零文件时，增加一次受限的安全加载兜底。
- [x] 最终 Answer 完全使用首轮预览文件时复用已读内容，避免重复读取磁盘。
- [x] 补充“哪里写错”“结果全是 0”等学生常用表达的本地问题类型判断。
- [x] 新增数据结构具体题目知识卡片：PA1 5 题、PA2 3 题、PA3 3 题、PA4 4 题。
- [x] 新增 Zuma 基础卡和代码 01 至 10 的错误子卡。
- [x] 新增独立 `problem-card-index.json`，不把具体题目节点混入概念型 `skill-graph.json`。
- [x] 工作区读取后按路径、题号、标题、特殊语句、代码标记和内容哈希进行本地候选召回。
- [x] 只有疑似已收录数据结构作业时才调用一次轻量题目识别 API。
- [x] 对识别结果校验候选 ID、置信度、独立证据和歧义，低置信度时不加载卡片。
- [x] 每轮最多加载一张主卡和一个同题错误子卡；失败时不加载整个题库兜底。
- [x] Answer 将卡片视为可选提示，并要求先用当前题面和真实代码验证。
- [x] 同一题目多轮追问复用卡片结果，题目变化或明确换题时重新识别。
- [x] 新增“正在识别当前题目”“正在读取相关题目提示”进度显示。
- [x] 将整个工作区路径与当前题目聚焦路径分开，避免大课程目录中的其他题目污染候选排序。
- [x] 题目识别入口读取知识索引中的题名、题号和别名，支持 Gift、Sect 等纯对话问题。
- [x] 精确代码哈希命中时，将错误子卡加入冻结 AnswerPlan，并以温度 0 生成答案。
- [x] 精确诊断摘要放在长工作区正文之后再次提交；普通题名匹配仍保持可选提示。
- [x] 将题目知识拆成匹配索引、结构化事实、教学 Markdown 三层，避免不同用途的数据混在同一文件。
- [x] 新增 `problem-card-facts.json`，覆盖 16 张基础卡和 10 张 Zuma 故障子卡。
- [x] 启动时校验结构化事实与题目索引一一对应，并拒绝重复、缺失和未知 ID。
- [x] Answer 每轮只接收已匹配题目和至多一个故障变体的结构化事实，不提交整个事实库。
- [x] 已验证样例、已排除误判和回答要求改为独立字段，精确哈希命中时用结构化主结论约束回答。
- [x] 重做聊天空白页，增加符合学生真实表达的快捷提问入口。
- [x] 输入框支持多行编辑、Enter 发送和 Shift+Enter 换行，并优化附件预览与移除操作。
- [x] 等待模型输出时在回答位置显示带动画的当前阶段，同时提供“停止”按钮。
- [x] 回答结束后可以展开查看本轮实际读取的工作区文件，便于确认题目和代码是否进入上下文。
- [x] 回答支持一键复制，Token 明细和工作区文件列表改为按需展开，减少界面干扰。
- [x] 增加“回到最新”按钮和更清楚的历史会话、模型设置与面板切换入口。
- [x] 修复 OpenAI 兼容推理模型消耗了输出 Token、但可见文本流为空时界面没有回答的问题。
- [x] Answer 默认关闭隐藏推理输出，把 Token 预算用于学生可见正文；空流重试成功后由控制器补发完整答案。
- [x] 真实 UI 测试发现并修复未验证 Zuma 故障卡编造具体样例的问题；`verifiedTests` 为空时只允许说明测试构造条件。

## 测试结果

- TypeScript 测试编译：通过。
- 扩展 Webpack 编译：通过。
- Webview 构建：通过。
- ESLint：通过。
- VS Code 扩展测试：`171 passing`。
- 2026-07-29 再次完整回归：TypeScript、Webpack、Webview、ESLint 和 `171` 项扩展测试全部通过。
- 2026-07-29 使用 OOP 回答效果测试集完成 98 次真实 API 测试：98 成功、0 失败、0 次零文件回答。
- 本轮真实 API 测试累计 1,511,969 tokens，平均首字 18.86 秒，平均总耗时 24.26 秒。
- 与修改前相比，零文件回答由 9 次降为 0 次；总 Token 增加约 42.9%，多轮问题类型继承偏强，需要继续优化。
- 专项验证：同一题目目录的 8 个相关文件可以全部进入加载流程。
- 真实目录检查：发现并补充了 `.in`、`.out` 输入输出样例支持。
- 新增专项验证：题目别名、最近题目选择、首轮小目录正文、自然追问继承、零文件兜底均通过。
- 2026-07-30 知识卡片完整真实 API 测试：55/55 成功，25 次识别、30 次复用、55/55 加载卡片。
- 知识卡片完整测试总计 655,473 tokens，平均总耗时 20.93 秒；相比无卡片基线总 Token 增加 6.68%，平均总耗时基本不变。
- 10 个 Zuma 文件全部精确命中正确子卡；最新逐题回归为 8 项正确、2 项部分正确、0 项跑偏，基线为 2 项正确、2 项部分正确、6 项跑偏。
- 2026-07-31 知识卡片与界面改进完整回归：`183 passing`、`1 pending`，TypeScript、Webpack、Webview 和 ESLint 均通过。
- 详细对比保存在本地测试资料中，不随产品仓库提交。

## 当前安全边界

- 工作区路径必须精确存在于扫描目录中。
- 不接受模型编造的路径、绝对路径或目录穿越。
- 文件数量没有固定上限，但单文件和总内容仍有容量上限。
- 必需的题目与当前代码优先，可选文件超过容量时按优先级跳过。
- 首轮 RouteAndPlan 正文另受 20 文件/300 KiB 或 10 文件/200 KiB 的更严格预算限制。
- 首轮文件正文属于不可信数据，不能作为系统指令执行。
- 不会把整个 Skill 参考库或整个工作区当作失败兜底。

## 下一步建议

1. 减少与当前提问无关的工作区文件上传，特别是简单概念或闲聊问题。
2. 进一步约束 RouteAndPlan 的结构化输出，减少格式回退。
3. 扩充知识卡片的已验证样例，并为新增卡片补充事实一致性测试。
4. 持续比较回答正确性、教学效果、Token 和首字耗时。

## 2026-07-31 GitHub PR 整理

- 移除仅供本机测评报告使用、且依赖外部数据目录的脚本。
- 移除重复的 V4 流程、性能记录和旧版 MVP 进展文件。
- 保留 V5 架构、题目知识卡片设计、源码、测试及运行时 Skill 数据。
- 补充 `.gitattributes`、GitHub Actions、完整忽略规则和 V5 更新日志。
- VSIX、`dist`、`out`、`node_modules`、`.vscode-test` 等可重新生成内容不进入仓库。

## 2026-08-01 算法内容理解 Skill 更新

- 新增“算法内容理解”专用回答模板：先解释算法解决什么问题，再用小例子逐步执行，最后补充复杂度、易错点和检查方法。
- 补充图算法知识：拓扑排序、Dijkstra、Prim、Kruskal、并查集和 Floyd。
- 补充树算法知识：树的遍历、二叉搜索树、AVL 旋转、伸展树和 B 树。
- 补充排序与字符串知识：归并排序、快速排序、稳定性、比较排序下界，以及不同 `next` 定义下的 KMP 逐步执行。
- 补充高频难点：单调栈、直方图最大矩形、建堆为何是 `O(n)`、开放寻址哈希表中的墓碑标记。
- 扩展 `skill-graph.json` 至 67 个节点，其中数据结构相关节点 43 个；新增内容均可由 Skill ID 独立选择和按需加载。
- 优化 RouteAndPlan 提示词和本地短语匹配，使“这个算法每一步怎么走”等初学者表达更容易命中算法内容节点。
- 完整本地回归测试通过：`185 passing`、`1 pending`；TypeScript、Webpack、Webview 和 ESLint 均通过。
- 算法内容理解专项真实 API 测试 65/65 调用成功；路由命中 58/65，专用模板命中 54/65，平均首字 6.81 秒，平均总耗时 11.60 秒，总计消耗 780,726 tokens。
- 当前已知问题：路由和模板命中仍有提升空间；U38、L05-4 暴露了反例校验不足与重复输出问题，后续需要加入回答正确性检查流程。
- 真实 API 原始问题、回答和详细评分保留在本地测试资料中，不随产品仓库提交。

## 2026-07-29 回答质量评分

- 使用既定 100 分体系完成 98 组双盲配对评分：正确性 60 分，教学效果 40 分。
- 裁判模型为 `DeepSeek-V4-Flash`；98 组全部评分成功，评分消耗 1,223,645 tokens。
- 按旧体系约定，单轮样本的“多轮一致性”默认记 5 分。
- 对 4 项可由原问题和工作区直接证实的裁判误判进行了人工修正，并在评分 JSON 中保留修正记录。
- 工作区规则更新前 V5：正确性 46.62/60，教学效果 31.23/40，总分 76.87/100。
- 工作区规则更新后 V5：正确性 51.52/60，教学效果 34.54/40，总分 85.82/100。
- 新版总分提高 8.95 分；明显或略好 42 项，持平 36 项，明显或略差 20 项。
- 基础题提升较小（78.97 → 81.09），半成品题、选择题、连问提升更明显。
- 当前主要问题集中在：用户明确索要完整代码时回答不完整、读取更多文件后回答重点偏移、少数回答对题目细节作过度推测。
- 完整评分报告保存在本地测试资料中，不随产品仓库提交。

## 2026-07-29 数据结构场景测试

- 新建 24 条数据结构测试：12 条“完全没思路”，12 条“半成品超时”。
- 均衡覆盖数据结构大工作区、单题工作区、无 `question.md` 三种环境。
- 真实 API 正式测试 24/24 成功，0 失败，耗时约 6 分 49 秒。
- 正式测试总计 425,711 tokens，平均每题 17,738 tokens。
- 平均首字 11.43 秒，平均总耗时 17.04 秒。
- “完全没思路”类型命中 12/12；“半成品超时”仅命中 4/12。
- 大工作区平均加载 8.1 个文件、39,237 tokens，存在同一 PA 邻题正文加载过多问题。
- 单题工作区和无题面工作区平均约 7,000 tokens，文件选择较稳定。
- 发现三项较严重回答问题：Temperature 错误推荐二维稀疏表、Zuma 错误认为 list
  基本可解决、Interview 未指出链表按位序寻址仍慢。
- DS009 首次 Answer 节点耗时 97.84 秒，单独复测总耗时 9.96 秒，判断为偶发 API 延迟。
- 详细记录和分析结论保存在本地测试资料中，不随产品仓库提交。
