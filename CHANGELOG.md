# Changelog

ClassMate 的重要变更记录在这里。

## [Unreleased]

- 课件结构感知分块与统一分词(课件利用率期 1):①抽取层保留结构边界——PDF 走 pdf-parse `pagerender` 逐页抽取(新增 `extractPdfPages`,looksScanned/truncated 语义保留),PPTX 按 `<p:sp>` 形状分组解析并识别 `title/ctrTitle` 占位符取 slide 标题(缺失回退首段并晋升,shape 外表格文本兜底),修复 D4;②分块以页/slide 为硬边界不跨单元合并,chunk 携带真实页号(D1 修复:pageEnd 不再恒为总页数;D2 消亡:删除字符比例估页),单元内超长段落才二次切分且每片独立成块(D3 修复:不再与前块拼接出 ≈2400 翻倍块),`CoursewareChunk` 新增可选 `title/unitLabel/sectionPath`;③词法层(设计 §5.1):新增统一分词器 `tokenizer.ts`——Intl.Segmenter('zh',word) 基线 + 课程术语表(约 130 条中英别名,glossary.ts)最长匹配叠加 + 子串抑制,chunk keywords 改为 标题×3/正文×1 加权取 top-12,频窗 n-gram 伪词(D5)全部废除;④图版本迁移(D10):`COURSEWARE_GRAPH_VERSION=2`,graphBuilder 写 version 2、mergeGraphs 取两侧较高版本;coursewareStore 读到 version<2 且有节点的旧图返回空图+`needsRebuild` 标记(用户拍板不自动重建),管理页显示「课件格式已升级,请重建搜索图」横幅,检索结果列表展示 title/slide 定位信息(打分语义未动,retriever.test 既有用例零修改)。前置验证:5 份测试课件逐页抽取 3 份 PDF 全部 100% 非空页、两份 PPTX 标题识别 16/16 与 107/107,未触发回退。
- tree-sitter kind 复用(#25):行内代码语义类型判定从本地正则切换到复用 7.3 落地的 Tree-sitter 符号索引;`inferSymbolKind` 优先按 `CppSymbol.kind` 映射(类→type、函数/方法→func、字段→var、宏→macro),索引不可用时回退原高置信正则;`sanitizeAnswerReferences` 异步构建并消费索引,`extractAnswerReferences` 同步等待。不改动 `ReferenceKind` 颜色接口与渲染层 CSS 类。
- 对比句型状态就近绑定(#29):修复 `answerGroundingValidator` 把转折/对比句中前后两个状态交叉绑到两个符号的误绑问题(如 "A已实现但B未实现" 原会把 "未实现" 也绑到 A、把 "已实现" 也绑到 B)。实现按转折连词(但/然而/不过/可是/却/反而等)切分句法分句,状态模式只匹配符号所在分句;无显式转折但含正负状态混合时按逗号/分号再切;消歧仍基于整句(限定容器/文件/行号),只把绑定域收窄到分句。新增 5 条单测覆盖转折、逗号、非对比枚举、文件消歧四种场景。
- 公式导入失真修复($N$ 导成 NNN,G5 三轮反馈):根因是 KaTeX/MathJax 把同一公式渲染成多份 DOM 副本——`.katex` 内有 `.katex-mathml`(隐藏 MathML 副本,mrow 正文 + `annotation[encoding="application/x-tex"]` 各一份文本)与 `.katex-html`(可见副本),content.js 通用 DOM 遍历把副本全部收集且丢掉 `$` 定界符,三份 "N" 相加正好产出 NNN。现 `extractMathSource` 在公式渲染根节点(`.katex-display`/`.katex`/`mjx-container`/原生 `<math>`;MathJax v2 走 `<script type="math/tex">` 源)输出唯一一份 LaTeX 源并停止下钻:优先取 annotation 原始码(不从渲染 DOM 反推),行内包 `$...$`、display 包 `$$...$$`;纯 MathML 无 annotation 时克隆摘除 annotation 后取单份文本。孤儿副本守卫兜底(选区起点落在公式内部克隆不到外层时):`.katex-mathml`/`mjx-assistive-mml` 直接丢弃、`.katex-html` 只留一份纯文本,保证任何路径不重复输出。浏览器侧无测试框架,验证走 `browser-extension/fixtures/katex-demo.html`(KaTeX CDN 渲染 $N$ 与 $$E=mc^2$$)+ README 手工步骤。
- 浏览器扩展入仓纳管:`code/classmate-browser-ext/`(仓库外普通目录,无版本控制)整体移入仓库 `browser-extension/`(8 文件逐字节比对一致后迁移);`.vscodeignore` 排除该目录(不膨胀 VSIX,分发方式仍为"加载已解压的扩展程序");eslint/tsconfig 无需改动——lint 脚本仅 `eslint src`、tsconfig 仅含 `src/**/*`,纯 MV3 JS 天然不在编译与 lint 边界内。文档路径引用同步(docs/add6-browser-extension-import.md、browser-extension/README.md)。Chrome 端需把「加载已解压的扩展程序」重新指向 `<classmate 仓库根>/browser-extension/`。
- 浏览器扩展导入仍不弹窗二次修复(G5 人审二轮反馈):**根因是激活时机断链**——`package.json` activationEvents 只有 onLanguage/onView/onCommand 触发器,用户装好/重启 VS Code 后若没打开过 C/C++ 文件、面板或跑过命令,`activate()` 从未执行,导入 server 根本不存在,浏览器侧 53135–53145 探测全部落空(上轮端口区间修复正确但不充分)。追加 `onStartupFinished` 一行使扩展随启动就绪(server 本身轻量)。配套:①extension.ts 新增状态栏自检项(纯代码零 manifest 改动):监听中显示 `$(plug) ClassMate 导入:<端口>`,失败显示 `$(error) ClassMate 导入离线`,点击走既有 showBrowserExtensionImportStatus 命令;②popup 点导入改为逐端口探测实时上屏(`53135 连接被拒 / … / 53137 ✓ 已连接`),全败给出分类文案与自查指引,命中后经 SW `importWithPort`(校验端口号)直连发送,popup 关闭不再影响请求;③background 拆出 `postImportToPort`、health 日志带响应体、POST 失败区分连接拒绝与服务端拒绝。单测新增:显式配置端口被占时 startServer 必须以 EADDRINUSE 显式 reject(不允许静默失败)。npm run test 518 passing / exit 0。
- 浏览器扩展导入保存弹窗修复(G5 人审反馈"点击导入后不弹保存窗口"):根因是两端端口发现失配——VS Code 端 `classmate.browserExtension.importPort` 默认 `0` 绑 OS 随机端口,浏览器扩展只探测固定区间 `53135–53145`,健康检查必然全部失败、请求从未到达 VS Code。现 `server.ts` 默认在探测区间内依次绑定第一个空闲端口(EADDRINUSE 逐个跳过),区间全满才回退随机端口并告警;显式配置端口行为不变。两侧补可观测性:VS Code 侧新增 OutputChannel `ClassMate Browser Import`(`src/browserExtensionImport/log.ts`),记录服务绑定策略/每个请求/校验拒绝/`showSaveDialog` 调用与结果/写文件成败;浏览器侧 background/popup/content 关键节点带 `[ClassMate import]`/`[ClassMate popup]`/`[ClassMate content]` 前缀日志。单测新增:合法请求必调一次 `showSaveDialog` 且参数含 README.md 默认值与 Markdown 过滤器、取消对话框不写文件断言加强、默认绑定落在探测区间(区间全占时 skip)、占用端口跳过、并发实例端口互异。
- ADD6 浏览器扩展题目导入(Chrome MV3):VS Code 端启动仅 127.0.0.1 监听的本地 HTTP 端点(`src/browserExtensionImport/server.ts`),提供 `GET /health` 与 `POST /import`;`POST /import` 调用原生 `showSaveDialog` 让用户选择 `README.md` 落点,写入 `vscode.workspace.fs.writeFile`,保存后自动打开文件。端口通过配置 `classmate.browserExtension.importPort` 控制,默认 `0` 随机分配,启动后写入 globalState。浏览器扩展位于 `code/classmate-browser-ext/`:内容脚本读取网页选区或正文并做轻量 HTML→Markdown 转换,popup 预览/编辑标题与 Markdown,右键菜单/导入按钮通过 background service worker 向本地端点发送请求;端口探测兼容随机端口与常见端口 53135–53145。参考 yorkxin/copy-as-markdown(MIT)架构思路自研转换,未引入 Turndown/Readability,第三方声明见 `code/classmate-browser-ext/THIRD-PARTY-NOTICES.md`。新增命令 `classmate.showBrowserExtensionImportStatus` 与单测 `src/test/browserExtensionImport.test.ts`。
- 课件删除真实生效与新语义(G6 二轮反馈):①删除按钮此前点了没反应——webview iframe 里 `window.confirm` 被静默禁用恒为假,`post('deleteCourseware')` 永远发不出去(panel 侧 handler 本身完整);确认弹窗改由扩展宿主原生模态框承担(`showWarningMessage modal`),webview 直发消息;②删除语义按用户拍板收窄:仅把课件移出导入列表(元数据随 workspaceState 持久化消失),**已构建的搜索图原样保留**、检索行为不变,只有点「重建搜索图」才按当前列表全量重建(空列表重建产出空图并显示明确状态提示;删除后不重建,重开 VS Code 列表保持已删、图保持旧版);③操作列删除按钮 CSS 加 `white-space: nowrap`,不再因列宽过窄逐字竖排;④用户可见文案统一:「重建 GraphRAG」→「重建搜索图」(管理页按钮/进度/确认框、导出命令的提示与保存窗口标题),内部标识符与开发者注释保留 GraphRAG 术语。单测:store/service 删除与重建语义 4 条(fake ExtensionContext + 临时目录)、管理页 webview HTML 回归 3 条(confirm 禁用锚点/文案/横排)。
- 中文课件检索失效修复与 PPTX 支持(G6 人审反馈):①检索单字 CJK 查询直接落空——`extractQueryTerms` 只生成 2–6 字 n-gram,查「树」「图」「环」得空词集提前返回 `[]`(真实课件实测:三份 PDF 抽取/分块/建图 35 节点 273 边全正常,多字查询「二叉树」「最短路径」均命中,唯单字归零);现单个 CJK 字符也进词集,内容命中改按出现次数计分(封顶 5 次)让单字查询按密度区分相关性;②PPTX 此前零支持(导入对话框只放行 pdf、service 只有 `importPdf`),新增 `pptxExtractor.ts`——Node 内置 zlib 手写 zip central directory 解析(store/deflate),按数字序读 `ppt/slides/slideN.xml`,段落 `<a:p>` 换行、文本 run `<a:t>` 拼接并解码 XML 实体,零新依赖;新增统一分发入口 `coursewareChunker.extractAndChunkCourseware` 按扩展名路由 PDF/PPTX 共用滑动窗口分块(`chunkExtractedText` 自 pdfChunker 提出复用),导入对话框与面板文案放开 PPTX;③新增命令面板调试命令 `classmate.exportCoursewareGraph`(Export Courseware Graph):把当前工作区已构建的图 JSON(节点/边/chunk 元数据)经原生保存窗口导出供人检查,未建图时给出明确提示。单测:retriever 单字查询/密度排序 2 条,pptx 抽取器 5 条(zip 构造器合成样本含 store/deflate/实体解码/乱序 slide,不拷真实课件)。
- 课件批量导入与 GraphRAG 检索(ADD6/本轨):新增独立 Courseware 面板(`classmate.openCoursewarePanel`)，支持 PDF 批量导入、删除、重建图、测试查询；PDF 提取复用 `pdfExtractor`，按滑动窗口分块；基于关键词共现(跨源)与同源顺序边构建轻量 GraphRAG 图，检索时做一次图传播；检索结果格式化后注入 Answer Prompt(`=== Imported courseware context ===` 固定标题)，无匹配时给出中性占位符；元数据存 `workspaceState`，图数据存 `globalStorageUri/classmate-courseware/<workspaceId>/graph.json`，不污染学生工作区。新增 `courseware/` 核心模块、`CoursewarePanel`、webview 管理页与 3 组单元测试。
- Activating 计时剖析(#15):新增低侵入 `ActivationProfiler`(src/activationProfiler.ts),在 `activate()` 体内插 16 个阶段 mark,开发/测试包默认启用、生产包强制关闭,结果落入 `ClassMate Performance` 输出通道;新增 `src/test/activationProfile.test.ts` 自动抓取基线并生成 `benchmark/activation-baseline-latest.{json,md}`。本轮 after-0803 dev/test bundle 的 `activate()` 体 total 基线 ≈122 ms(干净 profile 单轮),较 2026-08-21 同口径 ≈108 ms 略增,主要增量来自 Run 面板/GBK 解码等新增注册项;目标值维持 2026-08-21 拍板"不追进取目标",建议可选 dev/test 门禁 ≤150 ms。
- ADD5 引用符号按语义类型单独配色(#25 kind × 设置页):①kind 全集以代码为准 = `func|type|var|macro|std|other`(types.ts ReferenceKind;来源三路:tree-sitter 符号索引 KIND_BY_SYMBOL——class→type/function·method·constructor·destructor·operator→func/field→var/macro→macro,本地规则 inferSymbolKind 兜底,std 仅来自 LLM 提取 wire 的 t 字段);②设置页「界面主题」新增"引用·函数/类型/变量/宏/标准库/其他"一行一色(重置=回落内置语义色板),普通 Markdown 外链继续走单一「超链接颜色」,两者互不影响;③应用复用零重渲染机制:ClassMateTheme 平铺追加 6 个可选字段(refFuncColor…refOtherColor),applyClassMateTheme 写 `--classmate-ref-*` 六个新 CSS 变量,持久化(globalState)/广播(themeUpdate)/设置页载荷构造全走既有主题链路;④消费端是任务 7 已有的 `.kind-*` 行内语义色板(webview/classmate.css):静态值改为 `var(--classmate-ref-func, #dcdcaa)` 等 var() 回落形式(深/浅两套 fallback 各自保留),`.ref-code-link` 链接内 code-chip 即按 kind 显示用户色;新增 `.kind-other`(fallback inherit 维持中性)。向后兼容:旧配置无新键 → 变量为空 → 完全维持原色板;部分 kind 设色时其余回落。单测:theme 往返含 refFuncColor(回调+GET+globalState)、applyClassMateTheme 11 变量映射与空字段清理。

- ADD5 G5 复测修复:主题保存"完全无效"根因——设置页脚本只在页面加载(loadTheme)与重置按钮里写 `dataset.custom`,用户用取色器改色时没有任何 input 监听置位,提交载荷按 `el.dataset.custom ? el.value : ''` 恒为空串;服务端 parseThemeBody 跳过空字段后存了空对象、照样广播,webview 把 CSS 变量写成空串回退默认色——链路每环都"正常工作",唯独载荷是空的。修复:color input 加 `input` 监听置 `dataset.custom='1'`(程序化赋值不触发 input,"重置=清除字段"语义不变);单测只能锁存在监听(内嵌脚本行为无法在 server 测试执行,属测试盲区),真实改色→保存→当帧生效需人工复审。

- ADD5 G5 人审修订(保存成功勾动画 + 主题/模型配置即时生效):①设置页「保存主题」「保存模型配置」按钮在服务端确认保存成功后播放勾动画——✓ 从按钮左侧滑入盖住文字,停留约 2.5s 后滑出复原(纯 CSS transition);失败路径保留原有错误提示,不播勾。②主题即时生效收口:回答正文里的普通 Markdown 超链接颜色此前硬编码 `--vscode-textLink-foreground`,设置的"超链接颜色"对它无效——改为引用 `var(--classmate-link-color, …)`;webview 端主题不再经 React state,新增 `src/chat/classmateTheme.ts` 的 `applyClassMateTheme` 把 `themeUpdate` 推送就地写入文档根节点 CSS 自定义属性,由浏览器原生传播到所有 var() 引用处,已有控件零重渲染、零重挂载。③模型配置即时生效:设置页 POST /api/config 此前只落库,运行中 host 的 ChatSession `_llmConfig`/`_fallbackLLMConfig` 缓存不刷新,后续对话仍用旧 provider/model/url——server 新增 `onConfigSaved` 回调(配置只含 `apiKeySet` 布尔,key 本体永不下发页面),extension.ts 接线 `setLLMConfig`(内部向 webview 广播 llmConfig)+ `refreshFallbackLLMConfig`;API key 本身无缓存问题(LLM 调用时每次经 `_onGetApiKey` 读 SecretStorage)。单测:config 保存回调(含 key 不本体下发断言)与非法 body 不回调 2 条、applyClassMateTheme 变量映射/空字段回退/无 DOM 安全 3 条。

- ADD5 本地设置页（模型配置迁移 + 气泡/链接颜色自定义）:①新增 `src/settings/localSettingsServer.ts`，使用 Node 内置 `http` 绑定 `127.0.0.1:0` 动态端口，仅服务本地设置 HTML 页面；②所有 `/api/*` 端点强制校验 `X-ClassMate-Token`（128-bit 随机 hex，持久化存 `SecretStorage`）与 `Host` 白名单（仅 `127.0.0.1`/`localhost`），永不返回任何 `Access-Control-*` 头；③设置页从 URL query 读取 token 后立即 `replaceState` 抹除，后续请求带 header；④页面内可改 LLM provider/model/apiUrl/apiKey、备用模型，以及用户/助手气泡背景与文字、超链接颜色；⑤点击 Chat 面板设置按钮改为通过 `classmate.openLocalSettings` 命令打开本地页；⑥主题保存后通过 `ChatSession.broadcastThemeUpdate` 实时同步到所有 Chat 面板，MessageBubble 与 `.ref-code-link` 优先读取 CSS 变量、缺省回退 VS Code 默认色；⑦单测覆盖 token 错/缺、Host 伪造、无 CORS 头、主题/config 往返、token rotate 失效旧令牌。

- Run 面板与运行历史(#11 + ADD3 存储原语):①新增与 Chat Panel 同级的 Run 大标签页面板(`classmate.openRunPanel`,编辑器工具栏 Run 图标改为打开/聚焦面板,执行按钮在面板内),与 Chat **共享一个 React bundle** + `__CLASSMATE_ROUTE__` 路由切换(非独立 bundle);面板注册进 B1 的 panelGrouping 注册表,ADD2 预路由/relocation 兜底对 Run Panel 平权生效。②exe 发现分级链(grill R2-Q1):make 场景=解析 make 回显链接行 `-o <target>`(取最后一次出现)→ 根目录最新 `.exe` 兜底 → `showOpenDialog` 用户挑 → 无 exe 兜底文案;g++ 场景由 active 源文件推导(与 compilerService.resolveOutputPath 同语义),未编译提示"请先编译";默认不跑指定 make target。③stdin 运行前在面板填好、运行时一次性灌入(自动补结尾换行,管道保持打开);程序灌完后无输出等待更多输入超过 idle 窗口(默认 5s)即判定需要交互输入,主动结束进程并提示"需要交互输入,请在终端手动运行" + 一键"在集成终端运行",不再挂到硬超时报错。④运行历史(ADD3 原语落地,src/storage/runHistoryStore.ts):StorageUri 基座 + 逐工作区目录(hash 输入 = 工作区 Uri)+ 逐 exe 一条 JSONL 追加写;每 exe 保留最近 20 次环形;stdout/stderr 按 64KB 头尾截断(中间插"…N bytes 省略…"标记,按码点截不劈多字节字符);#14 错题本导出仅留只读接口(list/listExecutables/readAll),格式待 #14 立项。⑤运行器(src/run/runner.ts)自持有 stdin 灌入的 spawn 包装(含 Windows .cmd 同步 EINVAL 的 shell 回退),不改轨 A 的 compilerService。单测:discovery 10 条、runner 6 条(真实子进程)、history 10 条、webview HTML 1 条;宿主机能级 eval(eval-run/run-feature-eval.mjs)覆盖五风险点。
- ADD3 会话数据迁移到 StorageUri:新增 `src/chat/chatSessionStorage.ts`,按 `globalStorageUri/chat-sessions/<hash(工作区 Uri)>/conversations.json` 落盘;扩展激活时先把旧 `workspaceState` 中 `classmate.chatConversations.<workspaceId>` 的会话历史无损迁移到新目录,再切到 StorageUri 读写;旧 key 迁移完成后自动清空,避免重复迁移。会话历史不丢、同工作区目录与 run-history 对齐;新增 9 条单测覆盖 hash 目录、save/load 往返、幂等迁移、新存储已存在时保留新数据、损坏文件容忍。
- solution_request 教学策略守卫(#30):`solution_request` 不再允许返回完整代码/完整程序。实现:①规划层 `fallbackPlan`/`sanitizePlannerResult` 仅对 `code_edit` 保留 `allowCompleteCode=true`,`solution_request` 强制 `allowCompleteCode=false` 并向 `mustAvoid` 注入"不要直接给出完整代码或完整程序";②校验层 `validateStudentAnswer` 对 `solution_request` 独立限制代码块 >18 行即触发重生成/兜底;③回答提示词 `answerPromptBuilder` 显式声明 `solution_request` 禁止提供 full program,引导改为提示与追问;④上下文偏见 `biasRequestTypeForWorkspace` 不再把上一轮的 `solution_request` 继承到下一轮,自动回落到 `problem_hint`;⑤意图路由 `intentRouter` 补全"完整解法/给我解法/全部代码"等诉求信号。单测新增 11 条(规划 4/提示词 1/意图 1/上下文 1/图端到端 1);e2e 数据集 `eval-solution-request/` 含 5 case(4 单轮+1 多轮)与恶意模型回放脚本,验证模型若直接给完整代码会被兜底为不含代码块的 guidance。
- GBK 编码工作区磁盘读解码(#28):三处硬编码 `toString('utf8')` 的磁盘读(workspaceContextLoader 代码/文本加载、workspaceContextProvider 的 readFirstTextFile、classmateFileParser 的 CLASSMATE.md 解析)统一走新增 `decodeDiskTextFile`(src/workspace/textEncoding.ts)——jschardet 探测到 GB2312/GBK/GB18030 家族时用 iconv-lite 以 gb18030 解码(超集覆盖),UTF-8/ASCII/探测为 null 回退 utf8,与原行为逐字节一致(含 UTF-8 BOM 保留)。已打开文档仍走 `TextDocument.getText()` 不动。新增运行时依赖 jschardet + iconv-lite;单测 6 条(GBK 中文不乱码、UTF-8 不变、BOM 行为不变、ASCII、空/不可判定 buffer 回退不抛错)。
- G1 复审修订(面板状态机二轮):①compile_result.txt(classmate-output: 虚拟文档)active 时打开 Chat Panel 改为与其同分组——`resolveNewPanelColumn` 新增 `activeEditorIsClassMateOutput` 分支(extension.ts 按 activeTextEditor scheme 判定传入),虚拟文档在 Tab API 里本是普通 TabInputText,快照不按 scheme 过滤、所在组照常参与文件分组(回归单测锚定);②Debug Journey 高度(Panel 回 View / Open Chat 两场景回到"接近满高度")确诊:扩展代码无任何尺寸操纵,唯一声明是 package.json `initialSize` 2:1 权重,产品 schema(microsoft/vscode viewsExtensionPoint.ts)写明其"只在视图首次展示时生效";之后尺寸由 workbench 持久化恢复,@types/vscode 无任何运行时视图尺寸 API,内部 increase/decreaseViewSize 命令步长未公开且依赖焦点、无法确定性命中 1/3——判定为 VS Code 原生行为,不改码,最佳可替代行为见交接单 §7 交人审定夺。
- 面板分组决策泛化与 #18 闪屏收敛(Run Panel 前置):①新增 `ui/panelGrouping.ts` 统一三个分组决策纯函数——`resolveNewPanelColumn`(新建面板落列,自 ChatPanel 迁入)、`resolveFileOpenTarget`(ADD2 统一逻辑泛化到"任意 ClassMate 面板":无面板 active→跟随 active 组;有→面板之外已有文件的分组,无则对侧创建)、`resolveRelocationTarget`(挤占挪移目标);②新增 ClassMate 面板注册表(registerClassMatePanel/getActiveClassMatePanelColumn),ChatPanel 创建时登记、dispose 注销,分组判定不再绑死 ChatPanel,Run Panel 届时登记即复用;③`showTextDocumentRespectingPanels` 预路由助手落地到全部 ClassMate 发起的文件打开(引用跳转、应用修改、调试导出)——文件一步到位落进正确分组,不经过面板组,#18 闪屏在 ClassMate 路径上归零;外部打开(Explorer 点击等)仍由 ChatPanel relocation 并行 close+open 兜底(VS Code Tab API 无 onWillOpen/move 钩子,一帧残影为框架限制,证据见模块头注释)。分组/注册表/快照 13 条单测,chat 行为不变(决策表与原内联实现逐分支等价)。
- UI 小修(ADD1 + ADD7):①窄宽度下历史按钮隐藏(ADD1):ChatView/ChatPanel 宽度 ≤240px 时 `.history-toggle` 直接 `display: none`,避免极小宽度下文字+箭头按钮畸变;②"回到最新"按钮始终贴底(ADD7):将 `.jump-latest` 移出可滚动 `.classmate-messages`,与滚动区同置于新增 `.messages-shell` 相对定位容器,按钮绝对定位贴 `.messages-shell` 底部,不再受滚动内容或 padding 影响,在 ChatView 与 ChatPanel 中均保持贴底。
- 无 Makefile 编译聚焦当前文件(G4 拍板行为变更):无根目录 Makefile 时,g++ 路径从"编译同目录全部源文件"改为**仅编译当前 active 的 .c/.cpp 源文件**(教学场景聚焦当前文件;#9 阶段一命令预览同步只显示该文件,`resolveOutputPath` 沿用);active 为头文件/非 C/C++ 文件、或无打开文件时不编译,compile_result.txt 就地显示引导文案("请先打开要编译的 .c/.cpp 源文件,再点击 ClassMate Compile")。实现:`previewGppCommand`/`spawnGpp` 新增 `relatedSources` 选项(默认 true,`classmate.runCode` 保持旧的同目录多文件行为不变),新增 `isCompilableSourceFile` 扩展名守卫与 `buildNoCompilableSourceGuidance` 文案;有根目录 Makefile 的 make 路径完全不动。

- Makefile 构建与编译可视化(#8/#9):点 ClassMate Compile 时,若工作区根目录有 Makefile(大小写不敏感,只认根目录,不递归查找)则改用 make 构建——探测先 `make` 后 `mingw32-make`(复用 checkGppAvailability 的 Windows shell 回退),cwd=工作区根、无参默认 target,编译范围完全由 Makefile 决定;#9 时序为"点击→即时预创建 compile_result.txt 虚拟文件展示基本信息(构建工具/Makefile 路径/工作目录)→make 结束→强制刷新一次展示全量输出"。两个 make 都找不到时不报错,打开随扩展打包的预置文案 `resources/make-setup-guide.md`(只读虚拟文档,经 `classmate-output:` scheme)教用户配置 make;Makefile 只在活动文件所在子目录时提示放根目录后仍走原 g++ 单文件路径,无 Makefile 工作区行为完全不变。错误知识库新增 make 类两条(`make_no_rule`/"No rule to make target"、`make_missing_separator`/"missing separator",各配 stderr 样本单测),原 12 个概念不动;输出解析沿用 errorParser。compile/runCode 的编译结果事件记录提取为 `recordCompileOutcome` 共用。(注:本条所述"g++ 路径编同目录全部源文件"的行为已经 G4 拍板变更,见顶部"无 Makefile 编译聚焦当前文件"。)
- #9 可视化补齐与强刷修复(G4 复审):g++ 回退路径同样两阶段——点击即显基本信息(编译器/源文件/完整命令,经新增 `previewGppCommand` 不运行即解析 g++ 调用;两条编译路径共用 `buildCompileStartInfo` 文案),结束就地刷新全量;强刷修复为 `updateCompileOutput` 只 set 内容 + fire onDidChange(已打开的虚拟文档编辑器自动刷新)——原实现对同一 URI 重复 `showTextDocument`,分栏状态下会开出第二个 compile_result.txt。单测锁死:g++ 路径两阶段时序、同 URI 就地刷新不重复打开。

- 存在性声明接地核对(run17 全库审阅 + 人工判卷取证:206 轮 3 处真实事实错误,其中 2 处为"已实现的函数被称还没实现/需要补全"——残留 `// TODO:` 注释误导,7.7 第一期四类声明恰未覆盖此形态):①声明模式新增 existence 类(还没实现/尚未实现/需要补全/待补全),冲突判定为"实际已有代码"时拦截(重生成→仍冲突→本地事实提示,沿用 7.7 既有链路);②符号绑定升级为多符号 + 消歧——一句可谈论多个符号(枚举型回答常态),裸标识符参与绑定,同名多符号按限定容器(`Creature::printStatus`)→ 句内文件提及(player.h)→ 行号范围(第 102-105 行)逐级消歧,仍不唯一则放弃(宁缺毋滥保持);只有函数类符号参与绑定,类/字段无可核对函数体事实不参与(防"X 和 `Monster` 都重写了它"误伤);③NEGATION_GUARD 补"是否"疑问措辞。run17 两例错误原文内联为回归锚点(行号消歧命中 player.h printStatus/裸名+文件消歧命中 monster.h printStatus),另 9 条覆盖真阴性(空体上"还没实现"为真)、限定容器/文件消歧、疑问守卫、描述句("要实现两件事"非断言)、类符号不绑定。

- LLM 调用取消传导修复(run17 取证:一轮取消后挂起 700 秒才结束,被误分类为 provider_error):①`LLMRequest` 与 `streamResponse` 契约新增 signal,OpenAI/DeepSeek/Claude 三适配器把它传给 SDK `create(body, {signal})`——此前所有 HTTP 调用都不吃 signal,abort 后挂起的请求拦不断,只能等服务端超时;②`AdapterGraphModelClient` 非流式路径此前根本没把 signal 放进 LLMRequest(外层 aborted 检查只拦下一次调用,拦不住挂起中的请求),流式包装器也只 reject 自己的 Promise 而不拆底层流,现两条路径都把 signal 下沉到适配器;③SDK 拆断的 abort 在适配器流循环里统一归一为取消错误,不再被包装成 provider 失败(避免误触发 7.8 重试/备用 provider);④eval 取消分类同时认 AbortError 与"ClassMate request was cancelled"消息,取消不再混入 provider_error 统计。单测 5 条:非流式/流式/退化流式三条路径的 signal 传导与挂起请求秒级拆断 + 适配器级 SDK 二参断言(signal 不进请求体)。

- 引用提取截断修复(2026-08-20 真实会话"概述每个文件都有哪些方法"取证:回答正常但引用率归零,诊断导出显示 extract_references 响应 usage 恰为 600 token、JSON 在第 1505 字符条目中间被硬截断,解析失败又被空 catch 静默吞掉,`reference_extraction_failed` 从未触发):①提取调用 maxTokens 600→2000(全文件枚举型回答的提取响应可达 ~1.5k 字符);②新增 `salvageTruncatedReferences` 截断抢救——字符串感知的括号配平扫描找 r 数组内最后一个完整对象边界,闭合后重解析,截断前已完整的条目全部可用,截在半条的丢弃,逐条按 wire schema 校验、超 20 截断(与 schema 上限一致);③提取器的空 catch 移除,模型调用/解析失败上抛,由 ChatSession 现有 catch 记 `reference_extraction_failed`(含错误与回答上下文)后安全降级——回答不受影响,但失败不再静默归零。单测覆盖:截断抢救 6 条(含转义引号/花括号干扰、非法条目丢弃、20 条上限)+ extractor 4 条(maxTokens 断言、截断响应端到端出引用、不可抢救上抛、粗筛短路)。

- 引用标记竖线笔误兼容与防泄漏加固(7.9 run14 取证:bug1-single-27/34 两轮模型把标记写成 `{{ref|targetId|name}}`(ref 后是竖线非冒号),全链路只认冒号形态导致原始标记直达学生):①finalizer 的 MARKER/UNCLOSED 正则放宽为 `{{ref[:|]`,竖线标记与冒号形态走同一 targetId 校验/降级链——合法目标产出链接(契约指标是链接 recall,分隔符笔误不让学生失去可点击引用),坏目标照旧降级行内代码,流中断半截标记封口;②validateStudentAnswer/validateCorrectedAnswer 泄漏检查补"任何 `{{ref` 开头未转换标记即 invalid 触发重生成"兜底——将来再出现新语法变体最坏结果是重生成而非泄漏,普通花括号教学内容不误伤;③stripContractNotation 历史清洗同步剥竖线形态,防止已落盘回答进模型历史造成模仿扩散。run14 single-27 原始回答内联为回归锚点(零标记残留+引用清单产出)。

- 正确性检查修正版采用校验修复(7.9 run13 取证):correctness_check 拒绝候选回答后给出的修正版常是"多文件要点清单/纯文字指导"形态,此前用 `validateStudentAnswer` 的 code_edit 格式规则(恰好一个完整替换块、≤18 行)复核,把好的修正版二次否决成通用兜底(run13 全库 117 轮 3 例,占非 answered 的全部)。新增 `validateCorrectedAnswer` 只做结构底线检查(非空、不泄内部标签、不越提示层级代码量),修正版经底线检查即被采用为 answered;测试用 run13 两例真实修正版原文做回归锚点(严格校验确实拒绝/底线校验放行)。
- 判卷/test_harness 统计增强(7.9):新增 `src/eval/bug1Stats.ts` 与 `scripts/bug1-stats.mjs`——逐轮 token 用量(输入/输出/缓存命中率)、首字可见延迟(avg/p50/p95/max)、图耗时、逐节点与逐用例聚合,Markdown 报告一键导出;判卷页顶栏显示全局用量/延迟统计,逐轮卡片显示"输入(缓存命中%)/输出/首字可见/图耗时+逐节点明细";checkpoint 记录新增 `firstVisibleMs`(流式轮=真实首 token,引用契约缓冲交付轮=图完成时刻,修复旧 firstTokenMs 只覆盖流式轮 10/80 的盲区),eval zod schema 同步。

- 回答恢复通道(7.8):answer 节点模型调用失败(网络/断流,非取消)不再把裸 `[Error: ...]` 甩给学生——图内先按同一提示重试一次(仅零流出的失败可安全重试;已流出半截内容的转入兜底,在其后追加提示而不重播);重试仍失败交付 recovery_fallback 本地事实提示(新增 answerRecoveryHint:由冻结快照的 Tree-sitter 符号事实确定性生成,含道歉措辞、文件行范围、非空语句数,无符号索引时退化为引导核对文件,不含内部术语;validate/grounding/correctness 对程序生成的兜底提示不再复核)。备用 provider 显式配置后按次切换一次(llmConfig 新增 fallback 存取;设置面板新增备用区;FallbackGraphModelClient 包装主 client,失败时用备用重发同一请求,每轮最多一次,usage 按 `.fallback` 标签分开累计,切换事件进诊断)。缺代码证据程序侧补读最多两轮(新增 load_evidence_backfill 图节点与 evidenceBackfill 判定器:大工作区下用户以词干点名的未加载代码文件、或代码类问题零代码加载时,从 catalog 确定性补读并回 freeze_context 重建快照;补读目标不与已请求目标重复)。验证类问题(几行/写没写完/存不存在)的回答提示词显式声明"历史中关于代码状态的陈述不是证据,一律以冻结工作区为准"(自我事实污染防护);assistant 消息记录本轮依据的冻结 hash(basisFileHashes)并随历史传递,历史清洗的精确绑定从"仅引用文件"升级为"引用文件∪依据文件"——模型没打标记、块溯源也判 none 的轮次不再漏清洗。eval deliveryOutcome 新增 recovery_fallback。
- 事实核对与历史清洗的三处盲区修复(2026-08-20 真实会话"startTurn 翻车"取证:模型把注释掉的 startTurn 说成"✅ 已实现",证据正确但历史里的过期状态表未清洗、核对未拦截):①符号绑定容忍行内代码的调用形态——`` `startTurn()` ``、`` `takeTurn(Player &player)` ``、`` `Player::startTurn()` `` 取括号前标识符参与绑定(此前带括号直接绑不上,声明全部漏检);②completion 声明模式补"✅ 已实现/已实现/改好了"(负向 lookbehind 排除"未实现");③历史 STALE_STATE_PATTERN 同步补"已实现/改好了/⚠️ TODO"表格形态(含 emoji 变体选择符量词 bug 修复:⚠️ 的 ? 只作用于 VS16,裸全角括号里的 TODO 不匹配)。真实导出离线回归:turn10/turn11 的"已实现"声明均检出冲突,turn5 状态表清洗后"✅ 已实现"零残留。
- 措辞盲区补全(2026-08-20 run10 全 11 轮重现实测):completion 声明与历史 STALE_STATE_PATTERN 统一覆盖"已写好/已写完/已完成/已改好"(此前只认"已经写完了/已实现/改好了",run10 中 T4 状态表 28 处"✅ 已写好"全部漏网);run10 实测模型全程未翻转(T8 起正确说"是空的/注释状态"),数据集新增 bug1-multi-startturn-20260820 完整 11 轮回归 case(实现态概述→T8 前注释→三轮追问,复刻真实会话时序)。
- 回答事实接地校验(7.7 第一期):新增 grounding_check 图节点(verify_workspace 之后)与 answerGroundingValidator——模型回答中"只有注释/函数体是空的/有 N 行/已经写完"类结构事实声明,交付前用 Tree-sitter body 事实(commentOnly/empty/nonEmptyStatementCount)确定性核对;中文模式只定位候选声明句并按句内行内代码绑定唯一符号,真伪判定全依据事实,绑定不到唯一符号即跳过(宁缺毋滥)。冲突且回答未流出时,以一次性本地事实指令注入重生成(指令驱动条件边,answer 消费后自动复核);重生成仍冲突或无法重生成时交付 grounded_local_hint:程序按行范围生成的本地事实提示,含道歉措辞、不含内部术语;已流出内容不静默替换。eval 记录新增 groundingCheck(通过/冲突/重试数)与判卷页"事实核对"行;runner 级测试覆盖 T5(激活态误称注释→重生成纠正)与兜底路径(重生成仍冲突→本地事实提示)。
- 引用兜底策略升级(7.6 收尾):extract_references 节点一律调用(不再只在契约零标记时回退)——多轮后段模型标记衰减(run6 判卷实证)时仍能补引用。三层约束:提取清单预过滤掉模型已标记符号(小调用只看没提到的部分);合并时符号级/行级任一重合即丢弃提取版(冲突一律以模型 Answer 为准);严防二次引用(同一符号不因两家来源重复链接,含提取内部去重)。模型已提及全部可提取符号时短路跳过第二次模型调用(skipped: contract_covers_all_symbols)。
- 修复对话诊断导出范围过大:导出命令此前带出工作区**全部**会话聊天记录与**全部** journal 图事件(含其他会话的学生代码与对话);现改为只导出当前 active 会话的聊天记录与该会话(conversationId 匹配)的图节点事件,无会话归属的事件丢弃,导出文件 notes 同步说明范围。
- 修复真实扩展宿主(F5/dist 打包产物)下 Tree-sitter 符号索引必然降级的问题:旧逻辑按"源码布局"假设 `__dirname` 两层向上是项目根,webpack 打平后 `__dirname` 变成 `dist/`,基准高一级导致全部 wasm 候选落空(freeze_context 报 workspace_symbol_index_degraded,引用契约整链失效,且失败的 Promise 被缓存殃及会话后续所有轮次)。wasm 定位改为多基准(extensionPath 显式基准 > `__dirname` 本身 > `/..` > `/../..`),新增 `wasm/` 相对候选(打平布局);定位/加载失败不再缓存,下一轮可重试;GraphServices 新增 extensionPath 并由 extension.ts 注入,freeze_context 符号索引与路由结构图两个调用点转发。新增 dist 布局/源码布局/显式基准三组定位测试(此前 342 测试全在 out/ 布局上跑,属盲区)。
- 程序侧块来源自查(用户拍板:模型不再承担 refblock):新增 answerBlockSourceDetector,对回答中每个栅栏代码块做确定性溯源——块内有效代码行(去空白、剔纯符号噪声行)在冻结工作区已加载代码文件中按行集包含匹配,归到 Tree-sitter 函数行范围(嵌套取最内层符号)。三道防误配闸:命中行须为非注释有效代码;多行块需 ≥2 行有效命中(建议代码通常只有签名行同名,run5 离线验证:注释态快照下的建议补全代码全部正确判 none);单行块要求全工作区唯一有效出现。产出 `answerBlockSources` 证词数据(状态/文件/目标),不渲染、不进 answerReferences;块来源文件并入该轮 referenceFiles,历史清洗的文件归属升级为"标记+代码块实证"绑定;数据随消息持久化并进 eval 记录。提示词撤销对模型的 refblock 指示(保留"程序自行处理、禁自写链接"声明);finalizer 的 refblock 处理保留为防御路径。多行块来源的渲染(展示样式)延后至样式设计完成后(见 0803后要干的事情.md 待办)。
- 引用契约增补批次(判卷 run3 后,含用户三条指示):证词层新增 `{{refblock:targetId,...}}` 多行代码块来源契约——模型在代码块闭合栅栏后报告来源,finalizer 转为可见来源行(`*来源: [`takeTurn`](classmate-ref://0) · monster.h:26–31*`)并入清单,坏目标记 issue 降级不链接;模型自编的裸 `classmate-ref://` 链接(模仿历史所致)确定性剥成行内代码文字,消灭死链。历史清洗无条件剥成品链接/来源行(防模仿,与文件变化无关),文件变化轮的多行代码块连同来源行整体移除;旧提取路径 stripContractNotation 同步覆盖新形态。渲染层按用户边界重写:工作区 C/C++ 文件名(.cpp/.c/.h 等)任意提及可链(含纯文本,经 contextSummary.codeFiles 目录,文件级无行号链接),行内代码仅单标识符/限定名且唯一匹配,多对象代码(带参调用等)永不链。数据集 bug1-multi-real-20260818 修正为逐轮真实状态(T2 激活/T3 删空/T4 空/T5 补全,取证自真实导出 events)。
- 引用契约收尾(v2 计划):契约链接 URI 使用真实工作区根路径(GraphServices 注入 `workspaceRootUri`,相对路径逐段编码),点击打开正确文件;根路径缺失时标记降级为行内代码并记 `missing_root`,不再产出指向 `file:///w` 的死链。提示词规则改为"每个符号的每一次出现都要标记",示例展示同一符号两次被标记。新增渲染层保守补链(answerReferenceRenderer):仅对"已是行内代码且候选目录唯一匹配"的提及补视觉链接(`classmate-ref://N?i` 后缀区分),普通文字永不补链、同名不唯一不补、std:: 前缀不补;补链不回写引用清单,证词层 100% 来自模型标记;webview 点击携带 `inferred` 元数据。旧引用提取回退路径双防御:系统提示要求忽略 `{{ref:}}` 标记与 `classmate-ref://` 链接尾巴,提取前用 `stripContractNotation` 确定性剥离。eval 记录新增 `answerReferenceStats` 分层计数(标记链接/补链/清单长度),判卷页可见。
- 引用契约:冻结工作区时用 Tree-sitter 生成候选引用目标(targetId),随 Answer 提示词下发;模型在正文具体提及处放 `{{ref:targetId|名}}` 标记,程序确定性转换为行内代码链接与引用清单(目标文件/行号/语义色由程序决定,坏标记/过期 hash 降级为行内代码不链接,残缺标记不泄漏);带候选目标的回答改为缓冲交付;ChatSession 直接消费契约引用,跳过原第二次引用提取模型调用;历史消息携带该轮实际链接的文件,历史裁剪从词干猜测升级为精确文件绑定。
- 模型可见历史按工作区版本裁剪:图会话记录每轮实际加载文件的 hash;文件变化后,旧 assistant 消息中的代码块整体移除、"函数是空的/已经写完"等实现状态声明替换为旧版本占位说明,教学进度文字保留;历史按 token 预算从最旧整轮裁剪,最新一轮永不裁;UI 历史原样保留。
- 自适应工作区上下文:首次调用的范围选择增加确定性 token 预算(CJK 权重高于 ASCII),文件数达标但 token 超预算的大工作区不再全传;部分加载时,未进预览的代码文件以 Tree-sitter 符号结构图(仅签名)提交给路由,模型可按符号点名文件;Answer 提示词在用户问题前重述活动文件当前内容(问题相邻证据),缓解长上下文中证据被忽略的问题。
- 新增 Tree-sitter C/C++ 工作区符号索引:类、字段、方法、构造/析构、运算符、宏与顶层函数的结构化提取,含稳定 targetId、1-based 定义范围与函数体结构事实(空/仅注释/非空语句数/调用名单),供后续引用绑定与事实校验使用;语法文件 wasm 随扩展打包分发,解析失败按文件降级且不伪造符号。
- 工作区读取一致性修复:已打开文件优先读取编辑器缓冲区(未保存内容直达模型),加载条目记录 `bufferVersion`;Route 预览复用前按缓冲区版本复核,过期即重新加载。
- 快照 `snapshotId` 不再包含时间戳:同一份工作区内容重复捕获得到相同 ID,"内容未变"不再被误判为新版本。
- 新增整个可加载工作区的版本索引与变更检测:创建、修改、删除、重命名(同内容一删一建)均可准确识别,供跨轮与交付前复核使用。
- 新增 `verify_workspace` 图节点:回答交付前重新核对工作区;发现漂移且回答未流出时,重载相关文件、重建快照并按新内容重生成一次(仅一次),已流出的回答只记录漂移不静默替换。
- 新增完整对话诊断导出:可从命令面板或 `//export-diagnostics` 导出所有会话、每次模型请求/响应、每个图节点完整状态、工作区证据和引用提取结果,并自动剔除密钥字段。
- 新增 bug1 真实验收闭环:评测结果绑定模型实际看到的工作区证据与文件变更,并提供本地人工判卷页面、失效判卷检测和结构化兜底统计。
- 修复聊天输入框被后台状态同步误清空:只有显式携带草稿的广播才同步 composer,普通状态同步不再覆盖正在输入的内容。
- 容器状态机:ChatView 与 ChatPanel 不再同时出现,关闭 Debug Journey 不再影响 ChatView。
- 聊天被隐藏后,Debug Journey 工具栏出现"打开聊天"按钮,避免聊天失去可见入口。
- 侧边栏初始布局改为 ChatView 占 2/3、Debug Journey 占 1/3,避免挤压聊天框。
- 修复初始高度设置未生效:重命名侧边栏容器,使 2:1 权重在已有环境中首次打开即生效。
- Debug Journey 树不再显示当前工作区已不存在的文件,原始隐式日志保持不变。
- ChatPanel 为 active 标签时,新打开的文件自动落到另一个分屏,面板不再被盖住。
- 历史会话支持删除(× 按钮),历史切换按钮改为旋转箭头并美化。
- 回答中的代码/函数提及自动变成内联链接,点击跳转到对应文件(行/函数);纯文本复制不受影响。
- 回答中的行内代码按语义类型着色(函数/类型/变量/宏常量/std),代码片链接保留代码观感并增强悬停与键盘交互;默认色板对齐深浅主题语义色(自定义颜色接口见待办 #24)。
- 修复缓存命中/未命中统计恒为 0 的问题:provider 未返回缓存字段时不再显示 0,并新增 `//show-usage` 调试命令查看各节点原始用量。
- 重排 Answer 提示词为稳定内容在前(教学块/技能上下文/冻结工作区)、动态内容在后(答案计划/知识卡片),并把快照易变字段后置,提升 DeepSeek 前缀缓存命中率。
- 调试命令支持把输出写入文件:`//show-xxx <文件路径>` 写到指定文件并打开(相对路径基于工作区根目录),不带路径时仍输出到聊天。
- 新增 `//show-prompts` 调试命令:输出最近一次图流程中各节点真实发送给模型的完整提示词(可带文件路径写入文件);`//show-prompt` 仍只展示旧路径的 system prompt。
- 调试输出文件的相对路径固定落到项目根下的 `log/` 目录(开发态即 智理杯/log,绝对路径仍原样),并把 `log/` 目录从工作区上下文加载中排除,避免调试 dump 被当作作业文件回灌导致提示词膨胀。
- 引用提取(extract_references)输入瘦身:消歧清单只保留回答中显式提到的符号,每符号探测行数 8→6、每行文本 160→100 字符,文件清单挪到回答之前以提升跨轮前缀缓存命中率。
- 重排 answer/route 提示词为稳定字段在前:answer 的冻结工作区按路径稳定排序并移到技能上下文之前,route 的 manifest/preview 按路径排序并前置,提升同题追问的跨轮前缀缓存命中率。
- 冻结工作区与路由预览的文件正文改为原生代码块呈现(带 1 基行号、每文件一行元数据 JSON),不再用 JSON 转义内嵌,模型可直接读真实换行;Answer 提示词新增"存在但未加载"文件边界与省略计数,缺文件时允许明说需要哪个文件(方案见 plan-prompt-native-file-form.md)。

## [0.0.5]

- 优化作业目录识别与工作区上下文加载，支持按题目范围读取多个相关文件；
- 新增结构化数据结构题目知识卡片，并在回答前进行受控匹配；
- 重做聊天界面，增加快捷提问、多行输入、等待阶段、停止按钮和上下文文件展示；
- 修复模型产生结果但可见文本流为空时界面一直等待的问题；
- 为未经验证的知识卡片增加约束，避免编造具体输入、操作和期望输出；
- 补充工作区安全、知识卡片、LangGraph、提示词和 Webview 回归测试。

## [0.0.4]

- 引入 LangGraph RouteAndPlan、受控上下文加载和 Skill Graph 检索流程；
- 保留最终回答的流式输出，并增加首个 Token 前的阶段状态；
- 支持 `Makefile`、`GNUmakefile` 和 `*.mk` 工作区上下文；
- 增加代码修改预览、写入确认与文件冲突检查；
- 增加主要节点耗时与 Token 用量追踪。

## [0.0.1]

- 初始原型。
